# Copiá este archivo como .env.local y completá con los valores reales
# de tu proyecto Supabase (Project Settings → API).
# NUNCA pegues estos valores en el chat ni los commitees — .env.local ya
# está en .gitignore.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
